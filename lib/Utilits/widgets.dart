import 'package:flutter/material.dart';
  Widget SpaceBox({height, width, Widget, key}) {
    return SizedBox(
      height: height,
      width: width,
      child: Widget,
      key: key,
    );
  }

