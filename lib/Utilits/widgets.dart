import 'package:flutter/cupertino.dart';

class AppWidgets {
  Widget SpaceBox({height, width, Widget, key}) {
    return SizedBox(
      height: height,
      width: width,
      child: Widget,
      key: key,
    );
  }
}
